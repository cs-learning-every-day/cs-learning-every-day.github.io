
两个正整数相乘  大*小

```c++
#include <iostream>
#include <vector>

using namespace std;

vecotr<int> mul(vecotr<int> &A, int b) {
    vecotr<int> C;
    int t = 0;
    for (int i = 0; i < A.size() || t != 0; i++) {
        if (i < A.size()) {
            t += A[i] * b;
        }
        C.push_back(t % 10);
        t /= 10;
    }

    return C;
}

int main() {
    string a;
    int b;
    vecotr<int> A, vector<int> C;

    cin >> a >> b; // a = "123456"

    for (int  i = a.size() - 1; i >= 0; i--) {
        A.push_back(a[i] - '0'); // A = [6, 5, 4, 3, 2, 1]
    }
    C = mul(A, b);
    for (int i = C.size() - 1; i >= 0; i--) {
        printf("%d", c[i]);
    }
    return 0;
}
```