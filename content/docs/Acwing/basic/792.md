
两个正整数相减 A - B
考虑A < 0, B < 0  --> A - B = -(|A| + |B|)
考虑A > 0, B < 0  --> A - B = |A| + |B|

```c++
#include <iostream>
#include <vector>

using namespace std;

// 判断A>=B
bool cmp(vecotr<int> &A, vecotr<int> &B) {
    if (A.size() != B.size()) {
        return A.size() > B.size();
    }
    for (int i = A.size() - 1; i >= 0; i--) {
        if (A[i] != B[i]) {
            return A[i] > B[i];
        }
    }
    // 去掉前导0
    while (C.size() > 1 && C.back() == 0) {
        C.pop_back();
    }
    return true;
}

vecotr<int> sub(vecotr<int> &A, vecotr<int> &B) {
    vecotr<int> C;
    for (int i = 0, t = 0; i < A.size(); i++) {
        t = A[i] - t;
        if (i < B.size()) {
            t -= B[i];
        }
        c.push_back((t + 10) % 10);
        if (t < 0) {
            t = 1;
        } else {
            t = 0;
        }
    }
    return C;
}

int main() {
    string a, b;
    vecotr<int> A, B, C;

    cin >> a >> b; // a = "123456"

    for (int  i = a.size() - 1; i >= 0; i--) {
        A.push_back(a[i] - '0'); // A = [6, 5, 4, 3, 2, 1]
    }
    for (int  i = b.size() - 1; i >= 0; i--) {
        B.push_back(a[i] - '0');
    }
    
    if (cmp(A, B)) {
        C = sub(A, B);
        for (int i = C.size() - 1; i >= 0; i--) {
            printf("%d", c[i]);
        }
    } else {
        C = sub(B, A);
        printf("-");
        for (int i = C.size() - 1; i >= 0; i--) {
            printf("%d", c[i]);
        }
    }
    return 0;
}
```