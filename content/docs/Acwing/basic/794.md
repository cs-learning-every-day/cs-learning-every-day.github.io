
两个正整数相除  大/小

```c++
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

vecotr<int> div(vecotr<int> &A, int b, int &r) {
    vecotr<int> C;
    int t = 0;
    r = 0;
    for (int i = A.size(); i >= 0; i--) {
        r = r * 10 + A[i];
        C.push_back(r / b);
        r = r % b;
    }
    reverse(C.begin(), C.end());
    while (C.size() > 1 && C.back() == 0) {
        C.pop_back();
    }
    return C;
}

int main() {
    string a;
    int b;
    vecotr<int> A, vector<int> C;

    cin >> a >> b; // a = "123456"

    for (int  i = a.size() - 1; i >= 0; i--) {
        A.push_back(a[i] - '0'); // A = [6, 5, 4, 3, 2, 1]
    }
    int r;
    C = div(A, b, r);
    for (int i = C.size() - 1; i >= 0; i--) {
        printf("%d", c[i]);
    }
    cout << endl << r << endl;
    return 0;
}
```