
```c++
class Solution {
public:
    bool isAnagram(string s, string t) {
        vector<char> ms(26, 0), mt(26, 0);
        for (char c : s) ms[c - 'a']++;
        for (char c : t) mt[c - 'a']++;
        return ms == mt;
    }
};
```